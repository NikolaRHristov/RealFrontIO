// Add to capsule/src/main.rs:
// let admin_state = Arc::new(admin_api::AdminState { tx: tx.clone(), bounds: bounds.clone() });
// tokio::spawn(async move { admin_api::start_admin_server(admin_state).await; });